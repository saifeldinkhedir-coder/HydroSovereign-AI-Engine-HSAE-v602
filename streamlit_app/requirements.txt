# HSAE v6.0.14 (v602) - Streamlit Cloud requirements
# Only packages needed at startup. Heavy optional satellite packages
# (planetary-computer, rioxarray, pystac-client) are imported lazily at
# runtime and intentionally omitted so they never abort the install.

streamlit>=1.35.0
pandas>=2.0.0
numpy>=1.26.0
scipy>=1.12.0
plotly>=5.18.0
folium>=0.16.0
streamlit-folium>=0.20.0
scikit-learn>=1.4.0
requests>=2.31.0
openpyxl>=3.1.0
earthengine-api>=0.1.400
joblib>=1.3.0
hydrosovereign>=6.8.1
